# =============================================================================
#  requirements.txt — Dépendances pour l'image Docker de l'API FastAPI
# -----------------------------------------------------------------------------
#  Versions alignées avec docker/airflow/requirements.txt pour garantir la
#  compatibilité du modèle entre l'entraînement (Airflow) et le service
#  d'inférence (API). Tout désalignement majeur (numpy / scikit-learn) peut
#  empêcher le chargement d'un modèle MLflow via pyfunc.
# =============================================================================

# --- Stack ML (doit matcher Airflow) ---
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.8.0
mlflow==3.11.1

# --- API FastAPI ---
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
python-dotenv==1.2.2
